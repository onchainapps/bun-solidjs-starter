{
  "name": "bun-solidjs-starter",
  "version": "1.0.0",
  "description": "Bun + Elysia + SolidJS + Tailwind full-stack starter — clone and build.",
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/server.ts",
    "dev:ui": "cd frontend && vite",
    "build:ui": "cd frontend && vite build",
    "deploy": "bun run build:ui && bun run src/server.ts",
    "start": "bun run build:ui && pm2 start ecosystem.config.cjs",
    "stop": "pm2 stop bun-solidjs-starter",
    "restart": "pm2 restart bun-solidjs-starter",
    "logs": "pm2 logs bun-solidjs-starter",
    "setup": "bun install && cd frontend && bun install"
  },
  "dependencies": {
    "elysia": "^1.3.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
